# Testing Guidelines

- **DO NOT run automated browser testing or launch browser subagents.**
- The user will do manual testing only from here on.
- Rely on static analysis / syntax verification (`node -c`), clear documentation, and direct user review.
